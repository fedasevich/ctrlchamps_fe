import { styled } from '@mui/system';
import { Container } from 'src/components/appointments/styles';

export const StyledContainer = styled(Container)`
  gap: 0;
`;

export const StyledAccordionContainer = styled('div')`
  margin-top: 8px;
`;
